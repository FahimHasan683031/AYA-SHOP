export enum BOOKING_STATUS {
    PENDING = "pending",
    CONFIRMED = "confirmed",
    COMPLETED = "completed",
    CANCELLED = "cancelled",
}

export enum PAYMENT_STATUS {
    PENDING = "pending",
    PAID = "paid",
    FAILED = "failed",
}
